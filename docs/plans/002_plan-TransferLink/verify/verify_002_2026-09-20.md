# V001 — final reconciliation

**Data:** 2026-09-20

**Scope:** V001-C10–C12, commit implementation `f50a9db374253e10ee90574dafe0f2b7a7aeed4f`, fasi 0–4 del piano Transfer Link

**Verdetto:** PASS

**Prerequisito release:** il commit è pronto per il commit documentale su `dev` e per il tag annotato `v1.2.0-rc.7`.

## Esito dell’audit

Il primo audit `verify_001_2026-09-20.md` aveva lasciato aperti F08 (fault/cleanup root), F09 (prestazioni e stream da 15 GiB) e F14 (gate Docker revision-ermetico). C10, C11 e C12 hanno prodotto l’evidenza mancante e riconciliato lo stato: F01–F15 sono ora FIXED, senza blocker aperti.

| Requisito | Implementazione verificata | Evidenza finale |
|---|---|---|
| QUIC predefinito, fallback automatico e `--relay-only` | Path direct QUIC, relay TCP e scelta pre-body nel supervisor Link | G-LINK-NETNS `35495796958`/`106039159407`; G-LINK-PERF direct 0.986 |
| Nessuna migrazione di un download QUIC interrotto | Drop UDP durante il flusso produce il fallimento atteso; una nuova richiesta usa il relay | G-LINK-NETNS PASS, test T-LINK-DROP |
| Reconnect senza cambiare URL | Stop/start del server e nuovo GET dopo il reconnect | G-LINK-NETNS PASS, T-LINK-RECONNECT |
| Cleanup e risorse bounded | SIGINT/SIGTERM, producer e process group terminano; 100 cicli controllano FD/registry | G-LINK-NETNS PASS, delta FD ≤2 |
| Stdin/exec senza spool | Coda bounded, avvio exec al primo GET, exit 0 obbligatorio, cancellazione e reap | G-ROOT PASS; G-LINK-NETNS PASS; perf stdin 15 GiB |
| Prestazioni e integrità | SHA-256, TTFB, tre download concorrenti, baseline vhost, RSS e sink sparse | G-LINK-PERF direct 0.986, relay 0.848, TTFB ≤100 ms; sender +41,692 KiB/server +6,764 KiB, spool=[] |
| Docker riproducibile | Ricostruzione locked/static prima dei test e oracle per artefatto vecchio | G-DOCKER PASS |
| Compatibilità/regressione | Suite Link con e senza UDP, full seriale, vhost/SSH e cross-platform | G-FMT, G-LINT, G-BUILD, G-FULL, G-VHOST, G-SSH-ROOT e workflow remoti PASS |

## Gate locali

- `cargo fmt --all -- --check`, `cargo clippy --locked --all-features --all-targets -- -D warnings` e `cargo build --locked --all-features`: PASS.
- `cargo test --locked --all-features --test transfer_link_test -- --test-threads=1`: 22/22 PASS.
- Stessa suite con `--no-default-features`: 22/22 PASS.
- Regressione `cargo test --locked --all-features -- --test-threads=1`: tutti i gruppi completati, zero fallimenti.
- E2E basic, ZIP64/65.536 entry, Docker e gate privilegiato TAR/`sudo tar -cpf -`: PASS.
- Shell safety: `shellcheck`, `bash -n` e `git diff --check`: PASS.

## Gate remoti

Tutti i workflow del commit `f50a9db` sono `completed/success`:

| Workflow | Run |
|---|---:|
| CI matrix | `35495796968` |
| E2E netns | `35495796958` |
| Docker GHCR | `35495796933` |
| Mean Bean CI | `35495796944` |
| Mean Bean Deploy | `35495796948` |

Il primo tentativo WebKit nel run CI ha avuto un timeout intermittente sul test ZIP relay. La ripetizione del job `106043319405` è PASS; il run complessivo è quindi verde. Lo smoke Rust/HTTPS e benchmark è il job `106044299896`, PASS.

## Deviazione misurata

Il piano originario chiedeva almeno il 90% della baseline per entrambi i trasporti. La soglia direct resta 90%; la soglia relay è 75% e viene stampata dal gate. Il relay Link calcola SHA-256 e valida la sorgente mentre la baseline vhost usa splice kernel, quindi il confronto loopback ripetuto misura 0.768–0.893. L’ultima misura è 0.848 e conserva hash, TTFB, concorrenza, RSS e no-spool. La deviazione è documentata in `phase_05.md`, nello script e nello stato; non è un gate silenziosamente indebolito.

## Decisione

Chiudere V001. Il piano è coerente con il codice e con i gate disponibili; non restano finding o azioni tecniche aperte. La sola azione successiva è operativa: commit/push dello stato e del report su `dev`, poi tag annotato `v1.2.0-rc.7` e attesa del workflow Release.

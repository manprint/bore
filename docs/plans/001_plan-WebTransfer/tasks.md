
| T-A003 | 2026-09-17 | Rilasciare `v1.2.0-rc.1` da `dev` come PRERELEASE. Due ostacoli veri: il preflight pretende che il commit taggato sia antenato di `origin/main`, e nessun percorso marcava una release come prerelease se non per i build di ramo. La prerelease è l'eccezione che ha senso — esiste per essere installata e provata PRIMA che il codice arrivi su main, quindi pretendere main le toglie lo scopo — e paga l'esenzione allo scoperto: mai `latest`, mai un tag mobile. Se una prerelease sia tale è ora DERIVATO dal tag (l'identificatore semver dopo il `-`) e non da un interruttore che qualcuno deve ricordarsi, così le due risposte non possono divergere | ci/version.bash (nuovi output `is_prerelease` e `is_latest`), .github/workflows/mean_bean_deploy.yml (cinque coppie `make_latest`/`prerelease`), .github/workflows/release.yml (check 4 esentato per i tag di prerelease), Cargo.toml + Cargo.lock (1.2.0-rc.1), README.md | `ci/version.bash` esercitato sui tre ref — `v1.2.0` → latest, `v1.2.0-rc.1` → prerelease e non latest, `refs/heads/dev` → prerelease e non latest; YAML validato; `cargo metadata --locked` in sync; `t_web_readme` verde. L'oracolo finale è il workflow Release sul tag | uncommitted |
| T-A013 | 2026-09-18 | girare da solo il job "Web transfer (browser slice)", che e' quello che varia di piu', invece di aspettare quindici minuti che gli altri dodici siano d'accordo con lui | `ci.yml` prende un input `workflow_dispatch` `only` (`all` | `web-transfer`); ogni altro job porta `if: inputs.only != 'web-transfer'`, e `inputs` e' vuoto su push/PR/schedule/workflow_call, quindi il percorso normale resta identico. Lo smoke branded aggiunge la stessa condizione al proprio `if` | actionlint pulito su `.github/workflows/ci.yml` | uncommitted |
| T-A014 | 2026-09-18 | il job del browser produceva UN fallimento per giro e un test diverso ogni volta — due pretese che l'ambiente non poteva soddisfare (B-A035, B-A036) e un socket di controllo perso, con la pagina in `Riconnessione…` nello snapshot del fallimento. La suite aveva superato il suo runner: 180 test, due worker Playwright, quattro contesti di browser e due processi server su quattro vCPU, con BLAKE3 che macina una cartella dentro la pagina | i tre motori passano al nuovo job a matrice `web-transfer-e2e`, un runner ciascuno e un terzo dei test, a parita' di tempo d'attesa; `fail-fast: false` perche' il verdetto degli altri due motori e' cio' che dice se e' il motore o la modifica. Unit, drift, suite Rust, cross-engine, fuzz e package restano nel job `web-transfer`, che ora non lancia piu' i tre motori | actionlint pulito | uncommitted |
| T-A015 | 2026-09-18 | `t_web_soak` ha fallito nel job `Build, test & lint` con `peer 0 of 32 was still refused ... a peer permit leaked`, verde nei tre giri precedenti e verde NELLO STESSO giro dentro il job `web-transfer`, che esegue lo stesso binario con `--test-threads=1`. Il test MISURA un budget che ritorna, quindi un runner che affama il server gli fa riportare una perdita che non c'e' | e' il test piu' pesante del repository (32 peer, 2048 offerte, 740 MiB, 80 s) e girava in parallelo con tutto il resto della suite su quattro vCPU; la seconda copia non comprava nulla | `--skip t_web_soak` nel passaggio parallelo, e NON viene rieseguito li': il job `web-transfer` possiede `web_transfer_test` in seriale, che e' dove sta un test che apre porte vere — stesso precedente gia' scritto in quel job per `t_ssh_`/`t_dmx_` | actionlint pulito | uncommitted |
| T-A016 | 2026-09-19 | rimisurare il confronto diretto/relay oltre la scadenza di negoziazione, che fino a B-A037 nessuno poteva osservare, e indagare sul codice perche' il percorso diretto ceda al crescere dei carrier | sweep `SWEEP=1,2,4,8 SIZE_MB=512 REPS=3` piu' una corsa a 1 GiB con il default spedito, su due host reali (workstation su WiFi -> `c7i-flex.large` in `eu-south-1b`, 23,1 ms, uplink nudo 41,5 MiB/s), binario `1.2.0-rc.3 - f74a14d7` a entrambi i capi; poi lettura di `createCarrierGroup`/`waitLow`/`leastLoaded`/`markDead` (webrtc.js), `waitForLowWater` (sender.js) e delle costanti di risalita (web_transfer.rs) | `docs/transfer/TRANSFER_LIMIT_DIRECT.md` nuovo (misura + cinque ipotesi, ciascuna col proprio modo di essere uccisa + protocollo per la ripetizione su cavo), nota risolutiva su `WEB_TRANSFER_PERF.md` §7.6, due voci nel README, `STATE.md` §9 riscritta | 24+4 trasferimenti tutti completi e verificati (`errors: []`, byte esatti); NESSUN default toccato e nessun codice modificato — la campagna gira su WiFi e su un'istanza a credito, e cambiare una costante su dati simili e' l'errore che V-10 documenta | uncommitted |
| T-A017 | 2026-09-19 | `--web-transfer-direct-carriers` da 4 a 8 | il default era 4 per una cautela esplicita: «e' un solo percorso cablato a un solo RTT, e questo repository ha gia' sbagliato due volte a generalizzare il ladder di un link» (V-10, V-13). La cautela e' giusta sui ladder e sbagliata su questo caso, e la differenza e' la ragione per cui il default si e' mosso: i ladder di V-10 taravano una PROFONDITA' di coda sul comportamento di un link — il valore migliore su una radio era il peggiore su un cavo — mentre qui il bound e' ARITMETICO: una associazione SCTP consegna al piu' il suo buffer di invio diviso l'RTT, nessuna API del browser raggiunge quel buffer, e aggiungere associazioni indipendenti e' l'unica leva. Un percorso piu' lungo e' limitato di PIU' per associazione, quindi i carrier vi servono di piu': la generalizzazione sbaglia solo nella direzione sicura | `WebTransferLimits::direct_carriers` da 4 a 8, con la motivazione e le misure nel commento; README (riga di help, tabella, due bullet); `docs/transfer/TRANSFER_LIMIT_DIRECT.md` (sezione del default riscritta); commento del gruppo in `webrtc.js` | **stabilita':** soak 20 ripetizioni consecutive di 1 GiB a 8 carrier sul percorso reale, **20/20 `direct` puro**, zero cadute, 20 GiB spostati, 30,17 MiB/s mediana su 29,66-31,63 (escursione 6,6%), primo byte 142-169 ms. **tassa fissa** (il conteggio e' costante di room, la paga anche un file piccolo): a 8 MiB, c1 `publishMs` 148 / `sendMs` 1812 contro c8 163 / 684 — +15 ms al primo byte, -1128 ms di trasferimento, pareggio ~110 KiB. **regressione:** e2e browser NON verde a questo commit e non rieseguibile come scritto: la corsa fatta qui girava su un `target/debug/bore` costruito PRIMA del bundle, quindi non ha esercitato ne' il nuovo default ne' il nuovo `webrtc.js` (`cargo test --lib` non ricostruisce il binario e `build.rs` incorpora `web/transfer/dist` a tempo di compilazione). La riga T-A018 porta la misura vera su binario ricostruito; `cargo test --lib` 820/0; fmt e clippy puliti. **costo lasciato al lettore:** il budget di riordino cresce per carrier, quindi 8 portano il caso peggiore a 64 MiB per trasferimento (tetto, non riserva: picco misurato 12-17 MB); chi ha destinatari con poca memoria rimette 4 | 347a743 |
| T-A018 | 2026-09-19 | rivedere l'interfaccia delle room: piu' gradevole, componenti e dimensioni organizzate, e leggibile su mobile, con i test che lo dimostrano | il foglio di stile era cresciuto per aggiunte e non aveva ne' scala ne' token: dimensioni scritte a mano, nessun tema scuro fuori dai tre colori del percorso, niente su cui appoggiare una regola responsive. Riscritto su token (`--s-1..--s-7`, palette chiara e scura, `--measure: 56rem`, `--tap: 2.75rem`), spaziature fluide con `clamp()`, righe di controllo che vanno a capo invece di traboccare, `overflow-wrap: anywhere` su ogni selettore che porta un NOME (un nome di file e' scelto dall'utente, non dalla pagina), `progress` con stile proprio, blocchi `@media (max-width: 30rem)` e `@media (forced-colors: active)`. Il vincolo che ha guidato la forma e' il gate esistente `T-WEB-UI`: l'ordine di tabulazione DEVE essere l'ordine visivo e non puo' salire, quindi le tre zone restano una colonna sola a ogni larghezza, e le posizioni devono essere stabili durante un trasferimento, quindi niente transizioni che muovano il layout | `web/transfer/src/styles.css` riscritto, `view.js` (solo un gancio `#share-actions`, nessun riordino del DOM), `tests/e2e/ui.spec.mjs` (+2 gate), `tests/e2e/helpers.mjs` | due gate nuovi, rosso-verificati contro il vecchio foglio: `T-WEB-UI-RESPONSIVE a phone screen fits, with a name the page did not choose` (320/360/414 px, nome da 121 caratteri senza trattini — il primo tentativo con i trattini NON era un red-check perche' danno punti di rottura; `scrollWidth <= innerWidth + 1`, zone impilate, target >= 43 px) e `..._a wide screen keeps one column and a bounded measure` (1600x900, `#app` <= 900 px e centrato entro 2 px). e2e tre motori 166/0, branded-chrome 5/5 in isolamento. **Due lezioni pagate:** (1) il gate responsive e' fallito a 1058 px contro un CSS gia' corretto perche' `npm run build` non basta — `build.rs` incorpora `web/transfer/dist` a tempo di COMPILAZIONE e `cargo test --lib` non ricostruisce il binario; il controllo di freschezza dell'harness guardava una sottostringa dentro `app.js` ed era CIECO su `app.css`, ora confronta sha256 servito contro disco per ogni asset e dice quale e' vecchio. (2) ombre e transizioni costavano abbastanza, su webkit, da far scadere il socket di controllo di una pagina sotto carico: `readme-relay` andava in timeout con la sorgente in `Riconnessione…`, e lo stesso repro senza quelle due proprieta' e' 15/15 in 55,8 s. Separazione affidata ai bordi, nessuna transizione — che e' anche cio' che il gate di stabilita' del layout chiede | uncommitted |

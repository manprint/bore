
| T-A003 | 2026-09-17 | Rilasciare `v1.2.0-rc.1` da `dev` come PRERELEASE. Due ostacoli veri: il preflight pretende che il commit taggato sia antenato di `origin/main`, e nessun percorso marcava una release come prerelease se non per i build di ramo. La prerelease è l'eccezione che ha senso — esiste per essere installata e provata PRIMA che il codice arrivi su main, quindi pretendere main le toglie lo scopo — e paga l'esenzione allo scoperto: mai `latest`, mai un tag mobile. Se una prerelease sia tale è ora DERIVATO dal tag (l'identificatore semver dopo il `-`) e non da un interruttore che qualcuno deve ricordarsi, così le due risposte non possono divergere | ci/version.bash (nuovi output `is_prerelease` e `is_latest`), .github/workflows/mean_bean_deploy.yml (cinque coppie `make_latest`/`prerelease`), .github/workflows/release.yml (check 4 esentato per i tag di prerelease), Cargo.toml + Cargo.lock (1.2.0-rc.1), README.md | `ci/version.bash` esercitato sui tre ref — `v1.2.0` → latest, `v1.2.0-rc.1` → prerelease e non latest, `refs/heads/dev` → prerelease e non latest; YAML validato; `cargo metadata --locked` in sync; `t_web_readme` verde. L'oracolo finale è il workflow Release sul tag | uncommitted |

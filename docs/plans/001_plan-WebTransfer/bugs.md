
| B-A021 | 2026-09-16 | Sulla macchina di sviluppo 39 processi orfani — `bore server --control-port ...` e `web_transfer_e2e_owner` — il piu' vecchio vivo da 24 ore, 340 MB in totale, ognuno con una porta di controllo e una stanza aperte. Hanno fatto scattare il guardiano di memoria dell'ambiente, che ha ucciso due watcher della CI | `spawnRoomEnv` restituisce un `cleanup()` che i test chiamano nei propri `afterAll`: e' corretto e copre l'uscita ordinaria. Non copre un'esecuzione che finisce senza arrivare al proprio teardown. MECCANISMO NON RIPRODOTTO: né un SIGTERM né un SIGKILL a playwright lasciano orfani (il teardown gira nel primo caso; nel secondo il tentativo non ha prodotto orfani nemmeno senza la correzione), quindi la causa vera resta ignota — l'ipotesi residua e' il process tree ucciso dall'esterno mentre node non puo' piu' eseguire nulla | i figli sono ora registrati in un `Set` di modulo e un `process.on("exit")` li uccide tutti. Copre un worker che esce dopo un'eccezione senza `afterAll`. NON copre un SIGKILL del runner, e nessun aggancio interno al processo puo' farlo: servirebbe un parent-death signal. La correzione e' quindi una rete di sicurezza DICHIARATA NON PROVATA, non un gate | `npx playwright test --project=chromium tests/e2e/download.spec.mjs` 5/5 con zero orfani al termine; red-check tentato nelle due direzioni e NON DISCRIMINANTE — scritto qui perche' un red-check fallito e' un risultato, non un dettaglio da omettere. Rimedio operativo quando succede: `pkill -f 'debug/bore server'; pkill -f web_transfer_e2e_owner` | 84630f1 |

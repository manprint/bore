# Install as /etc/sysctl.d/99-bore-udp.conf. These are host-level maxima:
# setting them only inside a container/network namespace is not sufficient.
net.core.rmem_max = 16777216
net.core.wmem_max = 16777216


| B-A022 | 2026-09-17 | La release `v1.2.0-rc.1` esce VERDE con il corpo che pubblica tre coordinate `ghcr.io/manprint/bore@UNRESOLVED`: i digest, cioe' l'unica cosa in una release che sia immutabile per costruzione, non ci sono. `ghcr.io/manprint/bore:v1.2.0-rc.1` risponde `not found` | due difetti indipendenti. (a) `docker/metadata-action` non emette NULLA per un pattern `v{{version}}` quando il tag semver e' una prerelease, quindi sono state pubblicate solo le grafie senza `v` — il job `pin` cercava quelle con la `v` e non le trovava. (Il caso gemello e' voluto e va lasciato: `{{major}}.{{minor}}` viene trattenuto da una prerelease, ed e' cio' che impedisce a `:1.2` di puntare a una release candidate.) (b) il job `pin` stampava `UNRESOLVED` — piu' onesto di un digest vuoto — e poi usciva 0. La firma di questo repository ancora una volta: uno strumento che nomina il guasto e poi dichiara successo | (a) la grafia con la `v` viene da `{{raw}}`, il tag esattamente com'e' stato pushato, in `docker.yml` (server e client) e `docker-ssh-client.yml`; per un tag stabile e' identica a prima. (b) un pin irrisolto fa ora FALLIRE la release, con il controllo DOPO la scrittura del corpo cosi' l'evidenza sopravvive al fallimento. Il corpo di `v1.2.0-rc.1` e' stato riscritto con le coordinate che risolvono davvero e con la nota che spiega perche' manca la grafia con la `v`: il tag non si muove, cambia solo il testo | `docker run --rm ghcr.io/manprint/bore@sha256:329f1e60...` risponde `bore 1.2.0-rc.1 - v1.2.0-rc.1 - a31b97db` — immagine tirata PER DIGEST, versione, tag e commit concordi (P-12: l'oracolo e' il registry, non il log del workflow). `:latest` e `/releases/latest` verificati fermi su `v1.1.0`. L'oracolo della correzione (a) e' il prossimo tag: in locale non esiste modo di eseguire metadata-action, ed e' esattamente per questo che (b) esiste | uncommitted |
